export class ZoomConfig {
  static readonly MAX_SCALE = 1.5;
  static readonly MIN_SCALE = .5;
  static readonly SCALE_FACTOR = .05;
  static readonly WORKING_AREA_WIDTH = window.innerWidth * 0.94;
  static readonly WORKING_AREA_HEIGHT = window.innerHeight * 0.94;
}
